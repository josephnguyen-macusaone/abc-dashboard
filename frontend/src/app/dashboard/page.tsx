export default function Dashboard() {
  // Middleware performs the canonical redirect from /dashboard to the role path.
  // Keep this route minimal to avoid extra client-side redirect work.
  return null;
}
