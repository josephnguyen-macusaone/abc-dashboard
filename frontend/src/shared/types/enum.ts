export enum Role {
  ADMIN = 'admin',
  MANAGER = 'manager',
  STAFF = 'staff'
}

export enum SortBy {
  CREATED_AT = 'createdAt',
  EMAIL = 'email',
  USERNAME = 'username',
  DISPLAY_NAME = 'displayName',
  ROLE = 'role'
}

export enum SortOrder {
  ASC = 'asc',
  DESC = 'desc'
}
