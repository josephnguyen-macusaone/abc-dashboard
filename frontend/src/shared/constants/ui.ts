/**
 * UI/UX related constants
 */

import type { ColumnDefinition } from '@/shared/types';

/**
 * Theme Options
 */
export const THEMES = {
  LIGHT: 'light',
  DARK: 'dark',
  SYSTEM: 'system',
} as const;
