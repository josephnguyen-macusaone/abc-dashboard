# Prometheus datasource when Grafana runs in the same container (127.0.0.1:9090)
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://127.0.0.1:9090
    isDefault: false
    editable: false
